<div>
<ul>
<% @posts.each do |post| %>
    <li>
  <a href="#">
    <h2><%= post.title %></h2>
    <p><%= post.content %></p>
    <p><%= post.category.name %></p>
  </a>
    <button v-on:click="deleteBook" >削除します</button>
  </li>
<% end %>
</ul>

<div id='app'>
  <p>{{ message }}</p>
</div>


<div id='app2'>
  <p>{{ datetime }}</p>
</div>



</div>

<script src="https://cdn.jsdelivr.net/npm/vue@2.6.10/dist/vue.js"></script>
<script src="https://unpkg.com/axios/dist/axios.min.js"></script>

<script>
import axios from 'axios';


new Vue({
  el: '#app',
  data: {
    message: 'Hello VueJS!'
  }
  methods: {
    deleteBook: function  {
      axios.delete(`/memo/1`)
      .then(res => {
        # this.books = [];
        # this.bookInfo = '';
        # this.bookInfoBool = false;
        # this.fetchBooks();
      })
    }
  }
})

new Vue({
  el: '#app2',
  computed: {
    datetime: function(){
      return new Date();
    }
  }
})

</script>